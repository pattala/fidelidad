{
  "scripts": {
    "build": "npm run build:demo",
    "build:rampet": "npm run build:demo",
    "build:rampet:original": "node scripts/generate-client.js --client=rampet",
    "build:demo": "node scripts/generate-client.js --client=demo"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/cli": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "firebase-admin": "^13.6.0"
  }
}
